import 'dart:ui';
import 'package:music_player/const/colors.dart';

ourStyle({double? size = 14, color = whiteColor, weight = FontWeight.bold}) {
  return
    TextStyle(fontSize: size, color: color, fontWeight: weight);
}
